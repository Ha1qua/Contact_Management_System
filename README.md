# Contact_Management_System
Full Stack Contact Management System using Spring Boot (Backend) and React + Vite (Frontend). Includes CRUD operations, REST APIs, and PostgreSQL database integration.

PostgreSQL is used as the database. 
Set DB_USERNAME and DB_PASSWORD as environment variables in your system before starting the backend application.
